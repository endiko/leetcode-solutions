const piramide = (n) => {
  for(let row = 0; row < n; row++) {
    let level = '';
    for(let col = 0; col < 2 * n - 1; col++) {
      const mid = Math.floor(2 * n - 1 / 2);


    }
  }
}

/*
  - - # - -
  - # # # -
  # # # # #
*/
